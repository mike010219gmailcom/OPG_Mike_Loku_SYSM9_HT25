<Window x:Class="OPG_Mike_Loku_SYSM9_HT25.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OPG_Mike_Loku_SYSM9_HT25"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <StackPanel Orientation="Vertical" Grid.Row="1" Background="LightSkyBlue">
            <TextBlock Text="CookMaster" FontSize="54" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20"/>
            
            <TextBlock Text="Username" HorizontalAlignment="Center" />
            <TextBox Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Width="200" HorizontalAlignment="Center" Margin="0,5,0,5"/>

            <TextBlock Text="Password" HorizontalAlignment="Center"/>
            <TextBox Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Width="200" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                
            
            <Button Content="Login" Width="100" HorizontalAlignment="Center" Click="LoginMainWindow"/>
            <Button Content="Register" Width="100" HorizontalAlignment="Center" Margin="0,10,0,0" Click="Register" /> 

        </StackPanel>

    </Grid>
</Window>
