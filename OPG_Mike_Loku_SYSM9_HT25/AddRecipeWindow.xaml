<Window x:Class="OPG_Mike_Loku_SYSM9_HT25.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OPG_Mike_Loku_SYSM9_HT25"
        mc:Ignorable="d"
        Title="AddRecipeWindow" Height="450" Width="800">
    <Grid Background="LightBlue">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Titel:" FontWeight="Bold"/>
            <TextBox Text="{Binding NewRecipe.Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Ingredienser:" FontWeight="Bold"/>
            <TextBox Text="{Binding NewRecipe.Ingredients, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Kategori:" FontWeight="Bold"/>
            <TextBox Text="{Binding NewRecipe.Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="1" HorizontalAlignment="Center">
            <Button x:Name="SaveNewRecipe" Content="Spara Recept" Width="100" Height="25" 
                    HorizontalAlignment="Center" Margin="50" Click="SaveNewRecipe_Click"/>
            <Button x:Name="CancelAdd" Content="Avbryt" Width="100" Height="20" Click="CancelAdd_Click"/>

        </StackPanel>
        
    </Grid>
</Window>
